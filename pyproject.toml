[project]
name = "wztools"
version = "0.2.1"
description = "Default template for PDM package"
authors = [
    {name = "wzk", email = "no"},
]
dependencies = []
requires-python = "==3.11.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
